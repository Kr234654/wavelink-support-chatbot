# Address of the backend. For local development this is the default, so you can skip this file.
# After deploying the backend, put its https address here (no trailing slash).
VITE_API_URL=http://localhost:8000
